## 1. 狀態管理
- [ ] 1.1 在 `videoStore.ts` 中新增 `loop` 狀態（boolean）。
- [ ] 1.2 在 `videoStore.ts` 中新增 `toggleLoop` 方法，用於切換循環播放狀態。

## 2. UI 介面
- [ ] 2.1 在 `VideoPlayer.tsx` 控制區新增「循環播放」按鈕（位於時間軸附近）。
- [ ] 2.2 按鈕顯示循環圖示（例如：🔄 或類似圖示）。
- [ ] 2.3 按鈕在啟用循環時高亮顯示（例如：藍色背景），未啟用時為灰色。

## 3. 循環播放邏輯
- [ ] 3.1 監聽播放結束事件（`ended` 事件）。
- [ ] 3.2 當循環啟用且播放結束時：
    - 一般模式：跳轉到 0 秒並開始播放。
    - 預覽模式：跳轉到片段開始時間並開始播放。
- [ ] 3.3 確保循環邏輯與預覽模式的播放範圍限制協同工作。

## 4. 驗證
- [ ] 4.1 驗證一般模式下，啟用循環後播放到結束會自動跳回開始。
- [ ] 4.2 驗證預覽模式下，啟用循環後播放到片段結束會自動跳回片段開始。
- [ ] 4.3 驗證關閉循環後，播放到結束時正常停止。
- [ ] 4.4 驗證循環按鈕的視覺狀態正確反映當前循環狀態。
