## 1. 狀態管理優化
- [x] 1.1 在 `videoStore.ts` 中新增 `seekRequest` 狀態和 `seekTo` 方法。
- [x] 1.2 在 `videoStore.ts` 中新增 `playRequest` 狀態和 `play` 方法。
- [x] 1.3 在 `videoStore.ts` 中新增 `currentPreviewSegment` 狀態（儲存當前預覽的片段 ID 和名稱）。
- [x] 1.4 在 `videoStore.ts` 中新增 `clearPreview` 方法，用於清除預覽狀態並返回原影片模式。

## 2. 影片播放器增強
- [x] 2.1 在 `VideoPlayer.tsx` 中監聽 `seekRequest` 並執行影片跳轉。
- [x] 2.2 在 `VideoPlayer.tsx` 中監聽 `playRequest` 並執行影片播放。
- [x] 2.3 在 `VideoPlayer.tsx` 影片上方新增標題區域，顯示當前預覽片段名稱（或「原影片」）。
- [x] 2.4 在 `VideoPlayer.tsx` 控制區新增「返回原影片」按鈕，點擊時呼叫 `clearPreview`。
- [x] 2.5 實作預覽模式下的自動停止邏輯（當播放到片段結束時間時自動停止，可選）。

## 3. UI 介面更新
- [x] 3.1 在 `ClipList.tsx` 的表格操作列中新增「預覽」按鈕。
- [x] 3.2 實作點擊預覽按鈕時，呼叫 `seekTo` 和 `play` 方法，並設定 `currentPreviewSegment`。
- [ ] 3.3 (可選) 在 `ClipEditor.tsx` 的開始/結束時間輸入框旁新增「跳轉至此」小按鈕，方便確認時間點。

## 4. 驗證
- [x] 4.1 驗證點擊片段預覽時，播放器準確跳轉到開始時間。
- [x] 4.2 驗證播放器在跳轉後自動開始播放。
- [x] 4.3 驗證影片上方正確顯示當前預覽片段名稱。
- [x] 4.4 驗證點擊「返回原影片」按鈕時，清除預覽狀態並顯示「原影片」。
- [x] 4.5 驗證在預覽模式下，播放器標題區域顯示片段名稱；在正常模式下顯示「原影片」。

## 5. 時間軸範圍限制（方案 A）
- [x] 5.1 實作 getTimelineRange 函數計算時間軸範圍。
- [x] 5.2 修改進度百分比計算邏輯支援預覽模式。
- [x] 5.3 修改時間顯示邏輯顯示相對時間。
- [x] 5.4 修改進度條互動（點擊/拖動）支援預覽模式。
- [x] 5.5 實作播放範圍限制（到達片段結束時自動停止）。
